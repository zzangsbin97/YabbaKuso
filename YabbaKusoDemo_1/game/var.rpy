# 변수 모아둔 파일

init python:
    like_p = 0 # 교수 호감도
    like_h = 0 # 히로인 호감도
    strs = 0 # 스트레스
    attr = 0 # 매력
    rpt = 0 # 평판
    grd = 0 # 전공지식


init python:
    dissolve1 = Dissolve(1, alpha = True)

define fade = Fade(1.0, 0.0, 1.0)